名字: Docker Image CI/CD

on:
  push:
    分支: ["main"]
    paths:
      - "Dockerfile"
      - "channel_list/​**​"
      - "views/​**​"
  pull_request:  # 新增：PR时触发，用于代码审查
    分支: ["main"]
    paths:
      - "Dockerfile"
      - "channel_list/​**​"
      - "views/​**​"
  workflow_dispatch:  # 新增：手动触发选项[4,5](@ref)

env:
  DOCKER_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/pmn

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - 名字: Checkout code
        uses: actions/checkout@v4

      - 名字: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - 名字: Login to Docker Hub
        # 仅在push事件时登录，避免PR时误推送[4](@ref)
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          用户名: ${{ secrets.DOCKERHUB_USERNAME }}
          密码: ${{ secrets.DOCKERHUB_TOKEN }}

      - 名字: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name == 'push' }}  # PR时仅构建不推送
          标签: |
            ${{ env.DOCKER_IMAGE }}:latest
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
